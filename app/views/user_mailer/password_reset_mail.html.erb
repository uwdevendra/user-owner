<div style="color:red;">
  <% if @user.active? %>
    <%= t('.password_reset_message') %>
  <% else %>
    <%= t('.welcome_message', :organization_name => @user.organization.name) %>
  <% end %>
  <%# edit_password_reset_url(:id => @user.password_reset_token, :locale => 'en'%>
  <%= url_for(host: 'example.com',
			controller: 'password_resets',
            action: 'edit',
            only_path: false,
			:id => @user.password_reset_token, :locale => 'en'
            ) %>
</div>
